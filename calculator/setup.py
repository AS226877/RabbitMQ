# setup.py
from setuptools import setup, find_packages

setup(
    name='calculator',
    version='0.2',  # Updated version number
    packages=find_packages(),
    install_requires=[
        # any dependencies your library may have
    ],
)
